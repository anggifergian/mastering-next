/**
 * Framer Motion props need to know
 * 1. animate: define how animation should look like
 * 2. initial: define the inital state the animation or starting point
 * 3. exit: define animation when component exits
 */

export { default as FadeInAndOut } from './FadeInAndOut'